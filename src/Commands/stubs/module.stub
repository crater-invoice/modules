import SamplePage from './pages/SamplePage.vue'
import './index.css'

const myCallback = (app, router) => {
  router.addRoute({
    path: '/sample-page',
    component: SamplePage,
    name: 'sample-page'
  })
}

window.bootingCallbacks.push(myCallback)
